# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** I=ณัฐณิชา ปกแก้ว **รหัส:** 68543210063-2 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 14:00 | B1  | `Dashboard มี warning ตอนเรียก SummaryPanel เรื่อง unique key ควรไล่ตรวจจากตรงไหน`             | แนะนำให้ไล่จาก component ที่ Dashboard เรียกไปหา SummaryPanel แล้วตรวจส่วนที่ใช้ `.map()` ว่า element ที่สร้างแต่ละตัวมี `key` หรือไม่ | ไล่จาก `DashboardPage` → `SummaryPanel` พบว่า `items.map()` สร้าง `<article>` โดยไม่มี `key` จึงเพิ่ม key แล้วตรวจ Console |
| 14:10 | B1  | `ถ้าจำนวน Summary ไม่ตรงกับรายการ ควรไล่ตรวจอะไรบ้าง` | แนะนำให้ตรวจค่าของ `status` ในข้อมูลกับเงื่อนไขที่ใช้สำหรับนับแต่ละสถานะ| ตรวจข้อมูลจริงและเงื่อนไขการนับ แล้วแก้จุดที่ไม่ตรง จากนั้นทดสอบจำนวนอีกครั้ง                                              |
| 14:15 | B1  | `Filter ใน React ถ้าเลือกสถานะแล้วรายการไม่เปลี่ยน ควรตรวจตรงไหน`                              | แนะนำให้ตรวจค่าที่ส่งจากปุ่ม Filter และดูว่าเงื่อนไขกรองใช้ค่าเดียวกับ `request.status` หรือไม่ | ตรวจ `FilterBar` กับ `filteredRequests` พบว่าค่าตรงกัน แล้วทดสอบเลือก “เสร็จสิ้น”                                          |
| 14:30 | B1  | `ถ้าเปลี่ยน request id ใน URL แล้วหน้าเดิมยังแสดงข้อมูลเดิม ควรตรวจอะไร`                       | แนะนำให้ตรวจ `useParams()` และ dependency ของ `useEffect` ว่ามีการติดตาม id ที่เปลี่ยนหรือไม่                                          | ตรวจ detail page แล้วปรับให้ effect ทำงานเมื่อ id เปลี่ยน จากนั้นทดสอบ REQ-101 → REQ-102                                   |
| 15:00 | B1  | `หลังลบข้อมูลแล้ว requests.filter ขึ้นว่าไม่ใช่ function สาเหตุอาจมาจากอะไร`                   | แนะนำให้ตรวจชนิดข้อมูลที่ถูกส่งเข้า `setRequests()` โดยเฉพาะฟังก์ชันที่ทำงานแบบ asynchronous                                           | ตรวจ `deleteRequest()` พบว่าเป็น async จึงแก้การเรียกใช้งานและทดสอบการลบใหม่                                               |
| 15:10| B1  | `ถ้ากด Delete แล้วหน้าเว็บ crash ควรจัดการอย่างไร`                                             | แนะนำให้ตรวจ error flow ของฟังก์ชัน async และใช้ error handling เพื่อไม่ให้ component พังเมื่อเกิดข้อผิดพลาด                           | ตรวจ `handleDelete` และใช้ `try/catch` จากนั้นลองลบอีกครั้งและดู Console                                                   |
| 15:30 | B2  | `ถ้าจะเพิ่ม Search ใน Dashboard ควรออกแบบ state และการกรองข้อมูลอย่างไร`                       | แนะนำให้แยกข้อความค้นหาออกมาเป็น state แล้วนำไปใช้ร่วมกับข้อมูลรายการก่อนส่งให้ component แสดงผล                                       | เพิ่ม state สำหรับ search และสร้าง input จากนั้นทดสอบว่าข้อความที่พิมพ์เปลี่ยนผลลัพธ์                                      |
| 15:50| B2  | `ถ้าต้องค้นหาพร้อมกับ filter สถานะ ควรจัดลำดับการกรองอย่างไร`                                  | แนะนำให้สร้างผลลัพธ์จาก `requests` แล้วใช้เงื่อนไขสถานะและคำค้นหาในกระบวนการกรองเดียวกัน เพื่อไม่ให้ filter ตัวหนึ่งไปทับอีกตัว        | ปรับ logic ของ `filteredRequests` ให้รองรับทั้งสองเงื่อนไข แล้วทดสอบค้นหาพร้อมเลือกสถานะ                                   |
| 16:09 | B2  | `การค้นหาข้อความในข้อมูล React ควรระวังเรื่องตัวพิมพ์เล็กใหญ่และช่องว่างอย่างไร`               | แนะนำให้ทำข้อความที่นำมาเปรียบเทียบให้อยู่ในรูปแบบเดียวกันก่อนค้นหา เช่น ตัดช่องว่างและเปลี่ยนเป็นตัวพิมพ์เล็ก                         | ปรับข้อความค้นหาและข้อมูลให้เปรียบเทียบในรูปแบบเดียวกัน แล้วทดลองค้นหาด้วยรูปแบบตัวอักษรต่างกัน                            |
| 16:17 | B3  | `ถ้ามี function updateRequestStatus ให้แล้ว แต่ปุ่มรับเรื่องยังไม่ทำงาน ควรไล่ตรวจส่วนไหนก่อน` | แนะนำให้ตรวจเส้นทางข้อมูลตั้งแต่ปุ่ม → component → handler → service ว่ามีการส่ง `requestId` ครบหรือไม่                                | ไล่ตรวจ `RequestCard` → `RequestList` → `DashboardPage` แล้วเชื่อม callback ให้ครบ                                         |
| 16:25 | B3  | `ถ้ากดรับเรื่องแล้วข้อมูลใน service เปลี่ยน แต่หน้าจอยังไม่เปลี่ยน ควรแก้ที่ส่วนไหน`           | แนะนำให้ตรวจว่า component มีการอัปเดต React state ด้วยข้อมูลที่ service ส่งกลับมาหรือไม่                                               | ตรวจ handler แล้วนำข้อมูลชุดใหม่มา `setRequests()` จากนั้นทดสอบว่า Summary และสถานะบนการ์ดเปลี่ยน                          |
| 16:30 | B3  | `จะตรวจสอบอย่างไรว่าการเปลี่ยนสถานะ persist จริงและไม่ได้เปลี่ยนแค่หน้าจอ`                     | แนะนำให้ทดสอบโดย refresh หน้า แล้วตรวจสถานะอีกครั้ง เพราะการ persist ต้องเก็บข้อมูลไว้ใน storage                                       | กดรับเรื่องแล้ว refresh ด้วย F5 ตรวจสอบว่าสถานะยังเป็น in-progress จึงยืนยันว่าข้อมูลถูก persist                           |


*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ______________________
